# Minimal Tessera deployment. Apply with:  kubectl apply -k deploy/k8s
# See docs/getting-started.md for the full walkthrough (what to change + why).
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - configmap.yaml
  - deployment.yaml
  - networkpolicy.yaml
