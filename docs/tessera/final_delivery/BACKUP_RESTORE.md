# Final Backup and Restore

The final deployed backup command produced a separate online SQLite backup; verification reported integrity OK and schema v15. The backup PVC and live database persisted through full Deployment restarts. The existing encrypted OneDrive restic service completed a fresh snapshot on 2026-08-12 and its source includes the host configuration scope containing Tessera backups. An isolated execution-disabled restic restore was not performed because repository credentials are root-only; full off-node restore is therefore an explicit operations checkpoint, not a claimed delivery PASS. Pending or unknown Actions must not replay provider writes.