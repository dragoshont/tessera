# Regina Maria E2E

Mode: `REUSE_LOCAL_MCP`, pinned server contract `reginamaria-mcp/0.5.38`. The release requires both the operator mutation switch and Tessera's exact-action token before any provider write, and pins the compatible MCP SDK 1.28.1. Both isolated deployments are configured through Key Vault; no token value is exposed. User/wife ConnectedAccounts remain separate, and account B still requires its holder's independent login/MFA. Post-rollout no-token rejection and real owner-authorized reads remain required.