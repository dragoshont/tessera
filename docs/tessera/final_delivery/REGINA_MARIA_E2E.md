# Regina Maria E2E

Mode: `REUSE_LOCAL_MCP`, pinned server contract `reginamaria-mcp/0.5.36`. Two isolated MCP deployments exist. User/wife ConnectedAccounts remain separate; account B requires its holder's independent login/MFA. Real reads, isolation, availability and safe Action paths must be recorded without health details.