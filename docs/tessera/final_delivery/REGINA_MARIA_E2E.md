# Regina Maria E2E

Mode: `REUSE_LOCAL_MCP`, pinned server contract `reginamaria-mcp/0.5.38`. Both isolated deployments run digest `sha256:b51b7f13670bb1018b69fb335b176716b320122b82c200358bef78922035217a` with MCP SDK 1.28.1 and are healthy. Account A and account B each rejected a no-token cancellation locally with `mutating actions require Tessera exact-action authorization`; neither reached the provider. User/wife ConnectedAccounts remain separate, and account B still requires its holder's independent login/MFA. Real owner-authorized reads remain pending.