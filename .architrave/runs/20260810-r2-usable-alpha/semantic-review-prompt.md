You are the independent pre-implementation Adversarial Judge for Tessera R2 Phase 6 product UI. Read-only: do not write or mutate.

Ground in gates/rubric.md, architrave.config.json, knowledge/web.md, knowledge/yagni.md, the Phase 6 additions in .architrave/runs/20260810-r2-usable-alpha/{intake.md,tournament.md,recommended-plan.md,phase-ledger.md}, docs/ui/tessera-admin-portal-ui-spec.md, docs/ui/tessera-design-map.json, docs/tessera/r2/{R2_API_CONTRACT.md,ACTION_APPROVAL_UX.md,R2_PRODUCT_SPEC.md}, src/Tessera.Broker/R2ProductEndpoints.cs, and current web/src product source/stories/tests.

Evaluate the proposal to: (1) replace web/src/api/r2.ts with exact Page<T>, Problem Details, idempotency/version typed operations; (2) repair server response/body drift only where required for that authoritative API contract; (3) add isolated required product components/stories/design-map entries before route composition; (4) integrate exact Chat, Jobs, Accounts, Plugins, Memory, Activity, Settings workflows; (5) add focused Vitest/axe/keyboard tests and Playwright transport simulations for Remember→Why→Correct, Action→Approve→result, Job create/review/history/approval, plugin-disable blocking, and account revoke/recovery; and (6) update only evidence-backed R2 report/matrix claims. Existing backend/unrelated dirty changes are preserved; no fixtures enter production, no live calls/secrets/deploy/commit.

Revision 2 after judge feedback: also read `.architrave/runs/20260810-r2-usable-alpha/phase6-contract-matrix.md`, the revised authoritative R2 contracts, and revised ledger/summary/judge evidence. Exact `ModelProfileDto`, model-profile list/create, ConversationDetail, and Job detail are now defined. Replays preserve exact DTO bodies and use `Idempotency-Replayed`; receipt bodies retain `replayed`. Existing Chat/account/activity visual abstractions are explicitly extended/composed, not replaced by parallel routes. Type-to-confirm revoke plus Escape/focus return, hostile provider text, rapid duplicate, stale-version, secret non-echo, and Journey D evidence are required. The phase status now says reconciliation is active rather than backend-complete. Determine whether this proposal is sufficient to begin implementation; implementation gates remain pending.

Return the required rubric format with criteria, severity-ordered findings, required fixes, and exactly VERDICT: PASS, REVISE, or FAIL. This is a proposal gate, so future deterministic implementation evidence may be marked pending rather than failed.

Return the rubric's required acceptance checklist, dimensions, severity-ordered findings, uncovered specs, and `VERDICT: PASS | REVISE | FAIL`.
