# Intake

## Understanding

Finish the existing Tessera continuation against actual homelab truth: one canonical stateful server, Cloudflare Tunnel remote access, easy discovered setup, public/local integration search, working Web/macOS/iOS clients, deployment and real evidence. Preserve the current worktree and provider-neutral architecture.

## Acceptance Criteria

1. Stable descriptor verifies a TLS route before native auth.
2. LiteLLM setup is server-owned, truthful, idempotent and concurrency-safe.
3. Runtime readiness and per-user account authorization remain distinct.
4. Plugin search uses local, official MCP Registry and provider-owned public metadata adapters without arbitrary execution.
5. Core/Broker remain provider-neutral; public metadata is centrally downgraded.
6. Web desktop/phone, native iOS Release and installed macOS package pass deterministic checks.
7. Public source is PII/secret clean; private UUID, route and secrets stay in ignored/private config.
8. New image, private GitOps digest and Cloudflare route are deployed and observed before final status.

## Grounding Sources

- `architrave.config.json`, `AGENTS.md`, `gates/rubric.md`
- `docs/tessera/r2/*`, `docs/adr/*`, `docs/ui/tessera-design-map.json`
- actual public worktree, private GitOps worktree and read-only cluster/tunnel evidence
- controlling mandate in `/Users/dragoshont/Downloads/TESSERA_REALITY_AUDIT_AND_FINAL_FIX_MANDATE.md`

## Assumptions

- One Tessera replica remains the supported single-writer topology.
- Existing Cloudflare Tunnel, Flux, Traefik, GHCR and External Secrets patterns remain authoritative.
- Provider consent/MFA cannot be bypassed and does not block unrelated engineering work.

## Blocking Questions

None. The mandate explicitly authorizes Tessera-specific non-destructive deployment and packaging.
